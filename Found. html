<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Report Found Item - Smart Campus</title>

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        body {
            background: #f1fff5;
            min-height: 100vh;
            padding: 30px 15px;
        }

        .container {
            width: 100%;
            max-width: 650px;
            margin: auto;
            background: white;
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 5px 25px rgba(0, 0, 0, 0.15);
        }

        .logo {
            text-align: center;
            font-size: 45px;
        }

        h1 {
            text-align: center;
            margin-top: 8px;
            color: #14532d;
        }

        .subtitle {
            text-align: center;
            color: #666;
            margin: 8px 0 25px;
        }

        label {
            display: block;
            font-weight: bold;
            margin-top: 18px;
            margin-bottom: 7px;
        }

        input,
        textarea {
            width: 100%;
            padding: 13px;
            border: 1px solid #bbb;
            border-radius: 10px;
            font-size: 15px;
        }

        textarea {
            height: 100px;
            resize: vertical;
        }

        input:focus,
        textarea:focus {
            outline: none;
            border-color: #16a34a;
        }

        .preview {
            display: none;
            margin-top: 15px;
            text-align: center;
        }

        .preview img {
            max-width: 250px;
            max-height: 250px;
            border-radius: 12px;
            border: 2px solid #ddd;
        }

        .ai-info {
            background: #eaf8ef;
            padding: 15px;
            border-radius: 10px;
            margin
